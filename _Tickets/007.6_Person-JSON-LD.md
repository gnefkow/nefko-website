# 007.6 — Homepage Person JSON-LD

Sub-ticket of [007 - Improve AI Visibility](007_Improve-AI-visibility.md). Implements the v1 scope of Steps 7–8 (profile YAML + JSON-LD render).

## Goal

Add machine-readable `ProfilePage` + `Person` structured data to `/` and `/pages/for-ai-llm-readers/`, using YAML as the human-edited source of truth and Hugo to render JSON-LD at build time.

## Decisions

| Decision | Choice |
|---|---|
| Pages | Homepage and AI-reader page (same partial, both pages) |
| `jobTitle` | UX Strategy, Research, and Design Consultant |
| `knowsAbout` | Empty in v1 — Kyle fills in `data/profile/person.yaml` later |
| `alumniOf` | frog design only in v1 (universities deferred) |
| `Person.@id` | `https://nefko.xyz/#kyle-becker` (matches conversation article author) |

## Files

| File | Purpose |
|---|---|
| `data/profile/person.yaml` | Human-authored profile source data |
| `layouts/_partials/json-ld-profile.html` | Hugo partial — reads YAML, outputs JSON-LD |
| `layouts/_partials/head-additions.html` | Conditional include for target pages |

## Validation Checklist

- [x] `hugo --environment production` passes
- [x] `<script type="application/ld+json">` in `public/index.html`
- [x] Same block in `public/pages/for-ai-llm-readers/index.html`
- [x] `Person.@id` is `https://nefko.xyz/#kyle-becker` on both pages
- [ ] JSON validates at [validator.schema.org](https://validator.schema.org/) (paste build output after deploy)

## Out of Scope (future 007 work)

- `hasCredential`, full work history, testimonial/review schema
- `FAQPage` JSON-LD tied to visible Q&A on the AI-reader page
- `WebSite` entity block
- Populating `knowsAbout` (edit YAML when ready)
